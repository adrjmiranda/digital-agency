import { FunctionComponent, ReactNode } from 'react';

import * as Style from './style';

const Header: FunctionComponent = (): ReactNode => {
	return <Style.Container>Header</Style.Container>;
};

export default Header;
