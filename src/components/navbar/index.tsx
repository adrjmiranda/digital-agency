import { FunctionComponent, ReactNode } from 'react';

import * as Style from './style';

const Navbar: FunctionComponent = (): ReactNode => {
	return <Style.Container>Navbar</Style.Container>;
};

export default Navbar;
